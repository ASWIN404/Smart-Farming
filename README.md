# Smart-Farming
This project uses a Convolutional Neural Network (CNN) to classify plant leaf diseases from a labeled dataset. The model learns visual patterns from leaf images and achieves strong accuracy through preprocessing and data augmentation. It provides a simple and efficient approach for early disease detection in agriculture.
